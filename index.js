@inject( Service)
export class LMIndex {
  constructor( service) {


    this.service = service;

    this.activate();
  }
  activate(){
  service. getData().then((data) => {
  
  })
  }
  }